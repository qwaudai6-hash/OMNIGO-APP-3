<?xml version="1.0" encoding="utf-8"?>
<!-- AND01 + AND03: HTTPS only in production. Cleartext (HTTP) is permitted
     ONLY for local development hosts (Android emulator loopback 10.0.2.2 and
     localhost) so dart-define API_HOST dev runs keep working. All production
     traffic to the Railway domain and any other host is forced over TLS. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>

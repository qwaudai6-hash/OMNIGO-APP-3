{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -n \"$DATABASE_URL\" ] && [ -d /app/migrations ]; then echo \"=== Running migrations ===\" && for f in /app/migrations/*.sql; do echo \"Running $f...\" && psql \"$DATABASE_URL\" -f \"$f\" || true; done && echo \"=== Migrations complete ===\"; fi && /app/bin/monolith'",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
